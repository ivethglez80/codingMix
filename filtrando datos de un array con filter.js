function friend(friends){
    return friends
           .filter(ami=>ami.length === 4 );  
  }


  /* encontrar los nombres que tengan 4 caracteres y añadirlos a un nuevo array
     usando filter toma solo los que cumplan esa condicion y los añade a ese nuevo array*/